# Lazy.nvim lock file — tracked to pin plugin versions across machines.
# Run :Lazy update to refresh, then commit the updated lock file.

# Plugin data (managed by Lazy.nvim at runtime)
plugin/

# Neovim swap / undo / shada
*.swp
*.swo
*~

# macOS
.DS_Store
